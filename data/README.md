# Data

This folder is ignored by default to avoid committing large datasets.

Recommended approach:
- Put large files in `data/` locally (not committed).
- Commit only tiny sample datasets if needed (and document provenance/licensing).

